# familog (패밀로그) — 가족 공용 기록 앱

## 1. 프로젝트 목적

**가족의 기록이 서로에게 공유되고, 많은 기록을 할 수 있으며, 그 기록이 앞으로도 계속해서 이어질 수 있도록 하는 것. 세대를 넘어서도.**

기록한 내용은 내보내기(PDF/JSON/미디어)를 통해 이용자가 언제든 자유롭게 열람·활용할 수 있어야 한다. 특정 OS나 플랫폼에 종속되지 않는 표준 형식으로 제공한다.

## 2. 기획 방향 & 핵심 원칙

- **앱의 핵심은 기록과 공유다.** 가족끼리 정보를 공유하고, 오늘의 내용을 기록하는 것이 메인이다. 이 목적에서 벗어나는 기능은 넣지 않는다.
- **AI는 독립 기능이 아니다.** AI 비서 전용 탭은 없다. 캘린더, 가계부, 육아일지 등 각 기능 내부에 보조 힌트로만 존재한다.
- **로그인/인증은 후순위.** 현재 인증 가드는 비활성화 상태. 앱 내부 기능과 콘텐츠를 먼저 완성한 후 인증을 붙인다.
- **소비자 관점 UX 최우선.** 모든 작업의 기준은 "실제로 앱을 실행하는 소비자가 불편한 지점은 없는가"이다. 디자인 가독성, 배열 정렬, 간격, 색상 대비를 항상 검토한다.
- **데이터 영속성.** 기록은 세대를 넘어 이어져야 한다. 데이터 내보내기·백업·복원 기능은 핵심 기능이다.
- **말투는 가족이 서로에게 건네는 말이다.** 이 앱은 관리 도구가 아니라 가족이 함께 쓰는 기록장이다. 화면의 모든 글자는 시스템이 사용자에게 통보하는 말이 아니라 가족 구성원끼리 주고받는 말이어야 한다. → 상세는 §9 "말투 (보이스 & 톤)" 

### 작업 절차 (필수)
1. **작업을 마칠 때마다 이 CLAUDE.md 파일을 최신화한다.** 구현 현황(섹션 12)의 체크리스트를 업데이트하고, 새로 발견된 TODO가 있으면 추가한다.
2. **작업을 마칠 때마다 최소 1회 검증한다.** 웹(`expo start --web`)에서 실행하여 오류가 없는지 확인하고, 오류가 있으면 디버깅을 완료한 후에 작업을 마친다. 오류가 있는 상태로 커밋하지 않는다.
3. **검증 체크리스트:** 화면 렌더링 정상 여부, 라우팅 동작, 버튼/터치 반응, 콘솔 에러 없음을 확인한다.

## 3. 타겟 플랫폼

- Web, iOS (App Store), Android (Play Store)
- 단일 코드베이스: Expo (React Native) + Expo Router

## 4. 기술 스택

| 영역 | 기술 |
|------|------|
| 프레임워크 | Expo SDK 54 + Expo Router (파일 기반 라우팅) |
| 언어 | TypeScript |
| 상태관리 | TanStack React Query + Zustand |
| 백엔드 | Supabase (PostgreSQL + RLS + Auth + Storage + Edge Functions) |
| 모노레포 | Turborepo + pnpm |
| AI | Claude API (주력), OpenAI Whisper (음성 전사) — Edge Function 경유 |
| CI/CD | GitHub Actions + EAS Build/Submit + Vercel (웹) |

## 5. 프로젝트 구조

```
family-record-app/
├── apps/mobile/              # Expo 앱 (Web/iOS/Android)
│   ├── app/(auth)/           # 로그인, 회원가입 (현재 비활성화)
│   ├── app/(tabs)/           # 메인 탭 (홈, 캘린더, 기록, 설정)
│   │   ├── index.tsx         # 홈 대시보드
│   │   ├── calendar.tsx      # 공유 캘린더
│   │   ├── records/          # 기록 카테고리 (13개)
│   │   │   ├── index.tsx     # 기록 허브 (카테고리 목록)
│   │   │   ├── parenting.tsx # 육아일지
│   │   │   ├── reading.tsx   # 독서목록
│   │   │   ├── finance.tsx   # 가계부
│   │   │   └── [type].tsx    # 미구현 카테고리 플레이스홀더
│   │   ├── settings.tsx      # 설정
│   │   └── ai.tsx            # AI 비서 (탭에서 숨김, 보조용)
│   ├── app/settings/         # 설정 서브 화면
│   │   ├── members.tsx       # 가족 구성원
│   │   ├── profile.tsx       # 프로필 수정
│   │   ├── notifications.tsx # 알림 설정
│   │   ├── privacy.tsx       # 개인정보 보호
│   │   └── export.tsx        # 데이터 내보내기
│   └── app/onboarding.tsx    # 온보딩 (미활성화)
├── packages/core/            # 공유 비즈니스 로직
│   ├── src/supabase/         # Supabase 클라이언트, 인증
│   ├── src/utils/            # 역할 기반 권한 체크
│   └── src/types/            # DB 타입 정의
├── supabase/migrations/      # SQL 마이그레이션 + RLS 정책
└── CLAUDE.md                 # 이 파일
```

## 6. 하단 탭 구성

4개 탭: **홈** / **캘린더** / **기록** / **설정**
- AI 비서 탭은 `href: null`로 숨겨져 있다. AI는 각 기능 화면 내부에 보조 힌트로 삽입한다.

## 7. 홈 화면 레이아웃 규칙

위에서 아래 순서:
1. **헤더** — "우리 가족" 로고 + 알림 벨 (모달 팝업)
2. **인사말** — 시간대별 인사 + 날짜
3. **오늘의 일정** — 시간순 이벤트 카드 (캘린더 연결)
4. **최근 기록** — 2x2 그리드, 최대 4개, 최근에 쓴 기록 콘텐츠 노출

가족 구성원 아바타 섹션은 제거된 상태. 불필요한 콘텐츠를 넣지 않는다.

## 8. 기록 카테고리 (9개)

| # | 카테고리 | 상태 |
|---|---------|------|
| 1 | 육아 일기 | ✅ 상세 + 작성폼 + 상세모달 |
| 2 | 독서 목록 | ✅ 상세 + 작성폼 + 상세모달 |
| 3 | 가계부 | ✅ 상세 + 작성폼 + 상세모달 |
| 4 | 영화 관람 | ✅ 상세 + 작성폼 + 상세모달 |
| 5 | 여행 기록 | ✅ 상세 + 작성폼 + 상세모달 |
| 6 | 레시피 | ✅ 상세 + 작성폼 + 상세모달 |
| 7 | 가족 목표 | ✅ 상세 + 작성폼 + 상세모달 |
| 8 | 건강 기록 | ✅ 상세 + 작성폼 + 상세모달 |
| 9 | 타임캡슐 | ✅ 상세 + 작성폼 + 상세모달 |

> 제거된 카테고리: 음성/영상, 가계도, MBTI 기록, 디지털 유산

## 9. 디자인 시스템 (플랜테리어 톤)

### 컬러 팔레트
- 배경: `#F9F8F5` (오프화이트)
- Primary: `#4A8C6F` (세이지 그린)
- 액센트 배경: `#EFF6F1` (그린 틴트)
- 카드 배경: `#FFFFFF`, 테두리: `#EAEAEA`
- 텍스트 메인: `#1F1F1F`, 보조: `#4A4A4A`, 서브: `#888888`
- 위험/삭제: `#D94040`

### 브랜드
- 앱 이름: **familog** (소문자 영문)
- 로고 폰트: Gaegu Bold (한국어 손글씨 폰트)
- 로고 색상: `#2D5A3F`, 살짝 기울임 (`rotate: -2deg`)
- 본문 폰트: Pretendard / PretendardBold

### 말투 (보이스 & 톤)

**원칙: 시스템이 통보하지 않는다. 가족이 서로에게 건넨다.**

familog의 정체성은 "가족 구성원 모두가 사용하는 기록 앱, 함께한 기록이 사라지지 않고 계속 남는 곳"이다.
그래서 문구도 그렇게 들려야 한다. 무미건조한 분류 라벨 하나까지 포함해서.

| 쓰지 않는다 | 이렇게 쓴다 |
|---|---|
| 어려움 | 어렵지만 할 수 있어! |
| 보통 | 해볼 만해요 |
| 쉬움 | 금방 만들어요 |
| 데이터가 없습니다 | 아직 기록이 없어요 |
| 입력값이 올바르지 않습니다 | 금액을 입력해주세요 |
| 삭제되었습니다 | '주유소' 삭제됨 · 되돌리기 |

**특히 신경 쓸 자리**
1. **분류 라벨·상태값** — 난이도, 진행 상태, 우선순위처럼 건조해지기 쉬운 값
2. **빈 상태** — 아직 아무것도 없는 화면은 재촉이 아니라 권유로
3. **확인창·검증 메시지** — 사용자가 막힌 순간이다. 나무라지 말고 옆에서 거드는 말투로
4. **삭제·백업 문구** — "기록이 사라지지 않는다"가 이 앱의 약속이므로 안심을 주는 쪽으로

> ⚠️ **전면 톤 정리는 아직 남은 작업이다.** 새로 만드는 화면은 이 기준으로 쓰고,
> 기존 화면의 일괄 정리는 "말투 통일 스프린트"로 따로 진행한다. (아래 TODO 참조)

### FAB (Floating Action Button) 규칙
- `position: absolute, bottom: 16, right: 20, zIndex: 10`
- 탭바 위에 떠야 하므로 zIndex 필수
- 색상: `#4A8C6F`, 크기: 56x56, borderRadius: 28

## 10. 코딩 컨벤션

### 라우팅
- `_layout.tsx`에 모든 서브 라우트를 명시적으로 등록한다
- 네비게이션은 **상대경로** 사용: `router.push('./parenting')`, `router.push({ pathname: './[type]', params: { type: 'movies' } })`
- 절대경로 `/(tabs)/records/parenting` 형태는 중첩 Stack에서 오작동하므로 사용하지 않는다

### 스타일
- `StyleSheet.create()` 사용, 인라인 스타일 금지
- 카드에는 미세한 그림자 적용: `shadowOpacity: 0.03~0.04, elevation: 1`
- 그리드 아이템 너비는 `'47%'` 또는 `'48%'` 사용

### 컴포넌트
- 모든 터치 가능 요소에 `activeOpacity={0.7}` 적용
- 모달은 `animationType="slide"`, `transparent` 사용
- 빈 상태 UI를 항상 제공한다
- **알림/확인창은 `Alert.alert`을 쓰지 않는다.** react-native-web의 Alert는 빈 함수라 웹에서 무음이고 버튼 `onPress`도 실행되지 않는다. `components/AppAlert.tsx`의 `showAlert(title, message?, buttons?)`를 쓴다
- 작성 폼 모달 내용은 `<ScrollView style={{ maxHeight: 480~540 }}>`로 감싼다 (작은 화면에서 저장 버튼까지 닿도록)

## 11. Supabase 설정

### 테이블 구조

| 테이블 | 역할 |
|---|---|
| `families` / `family_members` | 가족과 구성원. 모든 격리의 기준 |
| **`records`** | **기록 9종 공용.** 공통 송장 + `data JSONB` 내용물 |
| `calendar_events` | 일정 (기록과 성격이 달라 따로) |
| `finance_settings` | 가계부 설정. 가족당 한 줄 |

> 카테고리별 테이블을 따로 두지 않는 이유와 상세는 `supabase/migrations/00002_create_records.sql` 상단 주석 참조.

### RLS (Row-Level Security)

모든 테이블에 `family_id` → 가족 단위 격리. 정책은 **반드시 `my_family_ids()`를 쓴다.**

```sql
CREATE POLICY "records_select" ON records
  FOR SELECT USING (family_id IN (SELECT my_family_ids()));
```

> 🔴 **이렇게 쓰면 안 된다 — 무한 재귀**
> ```sql
> -- family_members 정책 안에서 family_members를 다시 조회 → 그 조회에 또 같은 정책이 걸린다
> USING (family_id IN (SELECT family_id FROM family_members WHERE user_id = auth.uid()))
> --   ERROR: infinite recursion detected in policy for relation "family_members"
> ```
> `my_family_ids()`는 `SECURITY DEFINER` 함수라 정책을 건너뛰고 실행되어 이 고리를 끊는다.
> 함수 안에서 `auth.uid()`로 본인 것만 읽으므로 정보가 새지 않는다. (`00001` 참조)

### 중복 방지 (가계부)

`records(family_id, import_key)`에 부분 UNIQUE 인덱스가 걸려 있다.
앱 화면에서도 중복을 거르지만, **가족 여러 명이 같은 명세서를 동시에 넣는 경우는 화면 검사로 막을 수 없다.**
DB가 마지막 방어선이다.

### 역할 기반 접근 (⚠️ 아직 적용 안 됨)
- `admin` / `parent` / `elder` — 전체 접근 (재무, 건강 포함)
- `child` — 공유 콘텐츠만 (독서, 영화, 여행, 레시피, 목표)
- `guest` — 읽기 전용

> 규칙은 `packages/core/src/utils/permissions.ts`에 있지만 **RLS에는 아직 넣지 않았다.**
> 앱이 역할을 쓰지 않는 상태에서 정책을 먼저 조이면 개발 중에 계속 막히기 때문이다.
> 인증을 붙인 뒤 별도 마이그레이션으로 추가한다. **화면에서 숨기는 것만으로는 차단이 아니다 — DB 정책도 함께 걸 것.**

### iframe 환경 대응
Supabase 클라이언트는 `sessionStorage` 접근 차단 시 메모리 폴백을 사용한다 (`packages/core/src/supabase/client.ts`).

## 12. 구현 현황 & TODO

> 작업을 마칠 때마다 이 섹션을 최신화할 것.
> **큰 그림·환경 세팅·다중 PC 이어작업은 [`HANDOFF.md`](./HANDOFF.md)를 볼 것.** 이 문서는 상세 일지다.

### 완료된 작업
- [x] Turborepo + pnpm 모노레포 초기화
- [x] Expo Router 기반 앱 셸 (Web/iOS/Android)
- [x] 하단 4탭 구성 (홈/캘린더/기록/설정, AI 탭 숨김)
- [x] 홈 화면: 인사말 → 오늘의 일정 → 최근 기록 2x2 그리드
- [x] 홈 알림 팝업 모달 (읽음/미읽음 구분)
- [x] 캘린더: 월 이동, 날짜 선택, 이벤트 표시, 상세 모달, 일정 추가 폼
- [x] 기록 허브: 13개 카테고리 그리드 + 라우팅 연결
- [x] 육아일지 상세: 타임라인, 마일스톤, 자녀 필터, 통계
- [x] 독서목록 상세: 책 카드, 별점, 진행률, 상태 필터
- [x] 가계부 상세: 월별 요약, 카테고리 차트, 거래 목록, 필터
- [x] AI 보조 힌트 삽입 (캘린더, 가계부 내부)
- [x] 설정: 프로필 수정, 가족 구성원, 알림 설정, 개인정보 보호, 데이터 내보내기 화면
- [x] 로그인/회원가입 화면 (비밀번호 토글, 소셜 로그인 버튼)
- [x] packages/core: Supabase 클라이언트, 인증, 역할 기반 권한 체크
- [x] Supabase 마이그레이션: families, calendar_events, transactions, parenting_entries, reading_entries + RLS
- [x] iframe 환경 sessionStorage 폴백 처리
- [x] FAB 버튼 탭바 가림 해결 (zIndex)
- [x] 전체 화면 색상 대비 WCAG AA 기준 개선
- [x] CLAUDE.md 프로젝트 지침서 작성
- [x] 미구현 카테고리 10개 상세 화면 구현 (영화, 여행, 레시피, 음성/영상, 가족 목표, 건강 기록, 가계도, 타임캡슐, MBTI, 디지털 유산)
- [x] 기록 생성 폼 모달 9개 화면 구현 (FAB 버튼 연결) — ⚠️ 폼 UI만. 저장 연결은 아래 "기록 저장 스프린트" 참조
- [x] 기록 카드 터치 시 상세 모달 9개 화면 구현
- [x] 캘린더 일정 상세 모달 + 일정 추가 폼 (메모 포함)
- [x] 알림 모달 (작성자 표시, 커스텀 애니메이션)
- [x] 리브랜딩: familog + Gaegu 손글씨 폰트 + 그린 팔레트
- [x] 전체 화면 Pretendard 폰트 + 트렌디 스타일 통일
- [x] 홈 화면 빈 상태 UI (일정/기록 없을 때)
- [x] 구 색상 팔레트 잔존 일괄 정리

### 2026-04-28 UX 개선 스프린트
- [x] 홈 "최근 기록" 카드 → 해당 항목 상세로 딥링크 (육아일지/독서/레시피/가계부)
  - `expo-router` `useLocalSearchParams<{ openTitle?: string }>` + 자동 오픈 `useEffect` 패턴
- [x] 레시피 상세/작성 폼에 재료 목록·조리 순서·꿀팁 필드 확장 (ScrollView 래핑)
- [x] 여행 기록 상세에 여행 일지 본문 + 작성 폼에 일지 multiline 추가
- [x] 가족 목표 상세에 마일스톤 체크리스트 + 메모 표시
- [x] 프로필 사진 변경 팝업 — 카메라 / 사진첩 / 이모지 3옵션 + 16종 이모지 그리드 (`expo-image-picker`, web fallback)
- [x] 역할 라벨 UI를 `부 / 모 / 자녀` 3종으로 변경 (가족 구성원 색상 배지, 프로필 칩, 개인정보 문구 일괄 정리)
  - DB `FamilyRole` 타입(admin/parent/child/elder/guest)은 유지, UI 표기만 매핑
- [x] 새 여행 기록 폼 슬림화: 목적지 / 여행일자 / 여행 한 줄 소감 / 여행 일지
- [x] 독서 / 영화 / 건강 기록에 "기록 대상" 가족 구성원 셀렉터 도입
  - 각 화면에 `MEMBERS` + `CURRENT_USER` 상수, `recordedBy` 필드(작성자) 분리
  - 독서: 단일 선택 / 영화: 복수 선택 / 건강: 단일 선택
  - 상세 모달에 "작성자: 지수 (나)" 표시 → 알림 모달 작성자 노출 기반 마련
- [x] 읽는 중인 책 카드 인라인 편집 — 진척도 ±5% 버튼 + 직접 입력 + 메모 multiline
- [x] 편집 중 "독서 상태" 토글 (읽는 중 ↔ 완독); 완독 선택 시 ★ 평점 1~5점 선택기 노출, 완독 저장 시 progress=100·rating 일괄 갱신

### 2026-09-22 기록 저장 스프린트

**배경.** 화면 9개 모두 작성 폼 UI는 있었지만 저장 버튼이 `onPress={closeCreate}` — 모달만 닫고 아무것도 저장하지 않았다. 목록 데이터도 각 화면 파일에 `const ENTRIES = [...]`로 박혀 있어 `useState` 밖에 있었다.

**환경 복구 (선행 작업)**
- [x] 폴더명 `RECORD` → `dev-app-familog` 변경 후 `node_modules`가 옛 경로를 가리켜 앱이 부팅 불가였던 문제 해결
  - `node_modules/.modules.yaml`의 `virtualStoreDir`와 `@core/supabase` symlink가 모두 구 경로 참조
  - **해결:** `CI=true pnpm install` (linker 설정 변경 시 purge 확인 프롬프트를 TTY 없이 통과)
  - **교훈:** 프로젝트 폴더명을 바꾸면 `node_modules`를 지우고 재설치해야 한다
- [x] 없어진 경로를 가리키던 git worktree 2개 `git worktree prune`으로 정리
- [x] `.claude/launch.json` 추가 (preview용 expo web 실행 설정). `--clear` 플래그는 Windows에서 크래시(exit 3221226505)하므로 쓰지 말 것 — 캐시는 `%TEMP%/metro-cache` 수동 삭제

**공용 보관소 도입**
- [x] `apps/mobile/store/records.ts` — zustand 기반 기록 공용 스토어
  - 구조: 공통 송장(`id` / `category` / `title` / `createdAt` / `recordedBy`) + 카테고리별 자유 필드(`data`)
  - 제공 훅: `useRecordsByCategory(c)` / `useRecentRecords(n)` / `useRecordCounts()` / `relativeDay(ts)`
  - **주의:** 선택자 안에서 `filter`를 하면 매번 새 배열이 생겨 zustand v5에서 무한 리렌더가 난다. 반드시 `state.records`를 통째로 받고 `useMemo`로 거를 것
- [x] `apps/mobile/store/seed.ts` — 화면에 박혀 있던 예시 데이터를 스토어 형식으로 이전. Supabase 연결 시 이 파일과 `_layout.tsx`의 `seedRecords()` 호출을 함께 삭제
- [x] `apps/mobile/constants/family.ts` — 화면 3곳에 중복돼 있던 `MEMBERS` / `CURRENT_USER` 통합

**화면 연결 (9개 전부 완료)**
- [x] 여행 기록 — 목록·통계·필터를 스토어 기준으로 교체, 폼 `value`/`onChangeText` 연결, 상태(다녀옴/계획 중/가고 싶은) 선택기 추가, 빈 상태 UI, 미입력 항목 빈 줄 숨김
- [x] 영화 관람 — 위와 동일 + **폼에 없던 평점 선택기(★1~5) 신규 추가**, "평점 높은순"/"보고 싶은" 필터가 실제로 동작하도록 정렬 구현, 통계(총 관람·평균 평점·가족 함께) 실제 계산
- [x] 육아 일기 — 아이 선택기를 자유 입력 → 버튼 선택으로 교체, 날짜 자동 기입(`todayLabel()`), 마일스톤 쉼표 입력 → 배열 변환, 통계(총 기록·마일스톤) 실제 계산, 딥링크(`openTitle`)를 스토어 기준으로 이전
- [x] 독서 목록 — **배열 순번(index) 지목 → 기록 id 지목으로 전환** (목록 순서가 바뀌어도 엉뚱한 책을 고치지 않음). 기존 인라인 편집은 `patchRecordData`로 이전, 신규 등록 폼 저장 연결, 통계(총 도서·완독·읽는 중) 실제 계산
  - 이 전환으로 기존 타입 에러 2건이 함께 해소됨
- [x] 레시피 — 재료/조리순서 **여러 줄 입력 → 줄 단위 배열 변환**(`toLines`), 꿀팁 포함 저장 연결. "세대 전수" 통계는 제목+유래에서 `할머니|전수|물려` 등을 찾아 센다 (기존 하드코딩 3 → 실제 계산). **이 기준은 임의로 정한 것이므로 기획 의도와 다르면 조정 필요**
- [x] 가족 목표 — 마일스톤 여러 줄 입력 → 체크리스트 배열(`{label, done:false}`) 변환, 요약(전체·달성·진행) 실제 계산
- [x] 건강 기록 — 기록 대상 선택기 유지 + 저장 연결, 미입력 항목(메모·다음 검진일) 빈 줄 숨김
- [x] 타임캡슐 — 봉인일 자동 기입, 새 캡슐은 잠금 상태로 시작
- [x] **가계부 — 전면 재작성** (9/9 완료). 운영자가 직접 매일 쓸 핵심 기능이라 다른 화면보다 높은 기준을 적용했다
  - **구조:** 원래 `[{ date, items: [...] }]` 날짜별 이중 중첩이었으나 "거래 1건 = 기록 1건"으로 풀어 저장하고, 화면에서 날짜별로 다시 묶는다. Supabase `transactions` 테이블도 낱장 구조라 2단계에서 그대로 이어진다
  - **날짜는 반드시 `YYYY-MM-DD`.** 기존 `'4월 1일'` 같은 표시용 문자열은 정렬도 월별 집계도 불가능했다. 표시용 변환(`formatDay` → `9월 20일 (일)`)은 화면에서만 한다
  - 입력 편의: 카테고리 **버튼 선택**(자유 텍스트 폐지 — 오타·표기 흔들림 차단), 금액 **천단위 콤마 자동**, 날짜 기본값 오늘 + `오늘/어제/그제` 빠른 버튼, 결제수단 버튼(카드/현금/계좌이체), 내역 비우면 카테고리명으로 저장
  - 촘촘한 데이터: `type / amount / category / desc / date / method / memo` + 작성자
  - 회고 편의: **월 이동(◀ ▶)**, 월별 수입·지출·잔액 실제 계산, 카테고리별 차트(금액 + 비중 %, 금액순 정렬), **전월 대비 증감 실제 계산**, 날짜별 그룹에 그 날 지출 합계, 필터칩은 그 달에 실제로 쓴 카테고리만 노출
  - 하드코딩 제거: 월 이름, 수입/지출/잔액, 차트 비율, "곧 추가됩니다" Alert 3종, AI 힌트 문구가 모두 실제 계산값으로 교체됨
  - 시드는 이번 달·지난달 2개월치(17건)를 오늘 기준으로 생성해 전월 대비 비교가 바로 보인다

### 2026-09-22 가계부 2차 — 수정·삭제 + 입력 편의

> 운영자가 "가계부가 제일 핵심, 최고로 편리하게"라고 명시. 다른 화면보다 높은 기준을 적용한다.

**🔴 발견: `Alert.alert`은 웹에서 아무 일도 하지 않는다 (앱 전체 영향)**

react-native-web의 Alert 구현이 **빈 함수**다.
```js
// node_modules/react-native-web/dist/exports/Alert/index.js
class Alert { static alert() {} }
```
그래서 웹에서는 확인창이 뜨지 않고 **버튼의 `onPress`도 절대 실행되지 않는다.**
"정말 삭제할까요?" 같은 확인 절차가 통째로 무력화되어 삭제·로그아웃이 아예 동작하지 않았고,
"금액을 입력해주세요" 같은 검증 메시지도 전부 무음이어서 사용자는 왜 저장이 안 되는지 알 수 없었다.

- [x] `components/AppAlert.tsx` 신규 — `showAlert(title, message?, buttons?)` + `<AlertHost />`
  - `Alert.alert`과 시그니처 동일(`style: 'cancel' | 'destructive'` 지원)해서 그대로 바꿔 쓸 수 있다
  - `AlertHost`는 `app/_layout.tsx`에 한 번 올려둔다
  - 슬라이드 애니메이션을 쓰지 않으므로(`animationType="fade"`) 앞서 기록한 rAF 정지 함정에 걸리지 않는다
- [x] **`Alert.alert` → `showAlert` 21개 파일 47곳 일괄 교체**
- [x] **앞으로 `Alert` / `Alert.alert`을 쓰지 말 것.** `showAlert`를 쓴다

**가계부 수정·삭제**
- [x] 상세 모달에 **수정 / 한 번 더 / 삭제** 3버튼
  - **수정**: 작성 폼을 그대로 재사용해 값이 채워진 상태로 열린다 (편집 전용 폼을 따로 만들지 않음). 제목·버튼이 "거래 수정 / 수정 저장"으로 바뀜
  - **한 번 더**: 같은 가게를 또 갔을 때 값만 복사해 새 거래로. 날짜만 오늘로 되돌린다
  - **삭제**: 확인창 → 삭제 → **10초간 되돌리기 바** 노출 (Gmail 수준). 실수로 지워도 복구 가능
- [x] 거래 지목을 순번이 아니라 **id**로 (독서 화면과 같은 이유)

**입력 편의 (최고 우선)**
- [x] **자주 쓴 내역 칩** — 많이 쓴 순으로 6개. 탭 한 번에 내역·카테고리·결제수단·금액이 한꺼번에 채워진다. 가계부 입력의 대부분이 "같은 가게 반복"이라 이게 가장 큰 시간 절약
- [x] **과거 이력 자동 추론** — 내역을 적으면 같은 가게의 지난 카테고리·결제수단을 자동 선택. 사용자가 카테고리를 직접 고른 뒤에는 덮어쓰지 않는다(`categoryTouched`)
- [x] **쓴 사람 선택기** — 가족이 같이 쓸 때 누구 지출인지 구분
- [x] 월 요약에 **구성원별 지출** 표시 (지수 476,100원 / 민준 418,000원)

**CSV 가져오기 준비 — 데이터 모델 3개 필드 추가**
- [x] `ownerMember` — 돈을 쓴 사람(카드 명의자). 기존 `recordedBy`(기록한 사람)와 **분리**. 지수가 남편 카드 명세서를 대신 넣는 경우가 흔하므로 둘은 다른 정보다
- [x] `source: 'manual' | 'csv'` + `sourceFile` — 어떻게 들어온 기록인지. 상세 모달에 "명세서에서 가져옴" 배지 표시
- [x] `importKey` — **거래 지문.** `fingerprint()`가 `소유자|날짜|종류|금액|정리된가맹점명`으로 만든다. CSV를 같은 파일로 여러 번 넣어도 결과가 한 번 넣은 것과 같아지는(멱등성) 판정 기준
  - **수기 입력에도 같은 규칙으로 붙인다.** 그래야 CSV를 넣을 때 "이미 손으로 적은 거래"를 찾아낼 수 있다
- [x] `store/finance.ts` 신규 — 타입·카테고리·날짜 처리·지문·이력 추론을 한곳에. 화면/시드/향후 CSV 가져오기가 **같은 규칙**을 쓰도록 강제

**검증** (웹, 콘솔 에러 0건)
- 수정: 지우 소아과 15,000 → 18,500원, 월 지출 894,100 → 897,600 재계산
- 삭제: 확인창 표시 → 삭제 시 지출 879,100원으로 감소
- 되돌리기: 배달의민족 삭제 후 복원, 지출 894,100원 복귀
- 빠른 입력: 칩 탭 → 금액 87,400 · 내역 이마트 장보기 · 날짜 오늘 · 카테고리 식비 자동 채움

### 말투 통일 스프린트 (미착수, 우선순위 보통)
§9 "말투 (보이스 & 톤)" 기준으로 기존 화면 문구를 일괄 정리한다.
- [ ] 레시피 난이도 `쉬움/보통/어려움` → `금방 만들어요 / 해볼 만해요 / 어렵지만 할 수 있어!`
      (표시만 바꾸고 저장값은 유지 — 기존 기록이 깨지지 않게 표시용 매핑을 둘 것)
- [ ] 독서 상태, 여행 상태, 가족 목표 상태 등 나머지 분류 라벨
- [ ] 빈 상태 문구 9개 화면 재검토
- [ ] 검증·확인 메시지 전수 재검토

### 2026-09-22 가계부 3차 — 검색 / 반복 거래 / 예산

- [x] **검색** — 내역·메모·카테고리·쓴 사람에서 찾는다. 검색 중에는 **달 구분을 무시하고 전체에서**
      찾는다(달을 넘나들며 찾아야 의미가 있다). 결과 건수와 지출 합계를 함께 보여준다
      - 가맹점명은 `normalizeMerchant`로도 비교해서 `(주)`·공백·지점명이 달라도 잡힌다
- [x] **반복 거래** — 월급·관리비처럼 매달 같은 항목
      - 거래 상세의 **'매달'** 버튼으로 등록 (그 거래의 날짜에서 '매달 며칠'을 읽는다)
      - 그 달에 아직 안 넣은 반복 거래가 있으면 **배너로 알리고 한 번에 넣는다**
      - 중복 방지는 거래 지문(`fingerprint`)으로 — 이미 넣은 달에는 배너가 뜨지 않는다
      - 31일로 등록했는데 2월이면 말일로 맞춘다 (`recurringDateIn`)
      - 가계부 상단의 "매달 넣는 거래"에서 목록 확인·삭제
- [x] **예산** — 한 달 전체 + 카테고리별
      - 월 요약에 진행 바와 남은 금액. 넘으면 **"94,100원 넘었어요"**로 빨갛게
      - 카테고리별 예산을 정하면 차트의 % 자리에 `사용액/예산`이 뜨고 초과 시 빨갛게

**검증 (웹, 콘솔 에러 0건)**
- 검색: `이마트` 2건(9월·8월) 179,400원 / `학원` 2건 / `민준` 5건 — 달을 넘나들며 정확
- 예산: 80만원 설정 → 지출 894,100원 → **"94,100원 넘었어요"** (정확)
- 반복 거래: 관리비를 '매달 2일'로 등록 → 7월로 이동하니 배너 표시 →
  한 번 눌러 7월 2일에 185,000원 입력 → **배너 사라짐(중복 방지 확인)**

### 가계부 TODO (다음 단계)
- [ ] 결제내역 자동 가져오기 나머지 단계 — 아래 별도 섹션 참조
- [ ] 예산 대비 알림(푸시) — 푸시 연결 후

### 2026-09-22 카드 명세서 가져오기 (1단계 완료 — 신한카드)

운영자가 실제 신한카드 명세서를 제공해 그 구조에 맞춰 구현했다.

**실제 파일에서 확인한 구조 (신한카드)**
- **형식은 `.xlsx`** (CSV 아님). 시트명 `카드이용내역`, **헤더가 1행**(앞머리 없음)
- 13열: `거래일 | 카드구분 | 이용카드 | 가맹점명 | 업종 | 승인번호 | 금액 | 매입구분 | 이용구분 | 거래통화 | 최초결제일자 | 해외이용금액 | 취소상태`
- `거래일`은 `2026.09.21 10:46` (점 구분 + 시각) → `YYYY-MM-DD`로 변환
- `금액`은 숫자(float). **취소 건은 음수**
- **마지막 줄은 `총 12건` 합계 행** → 반드시 건너뛴다
- `업종`은 비어 있음 → 카테고리 자동분류는 **가맹점명으로만** 가능
- 해외 결제는 `금액`이 이미 원화 (거래통화/해외이용금액은 참고용)

**🔑 설계를 바꾼 두 가지 발견**
1. **한 파일에 카드가 여러 장 섞인다.** 실제 파일에 `본인112* / 969* / 380* / 099*` 4장이 있었다.
   "이 파일은 누구 카드인가요?"를 한 번 묻는 설계로는 안 된다 →
   **카드 뒷자리 → 가족 구성원 매핑**으로 구현. 3명이 각자 알아서 갈린다
2. **취소 건은 빼야 한다.** 택시 가승인(-14,100) 취소 + 실제 결제(13,900)가 함께 찍힌다.
   근거: 파일 하단 합계 `1,136,172원`이 **양수 행만 더한 값**과 정확히 일치 —
   신한카드 자신도 취소를 합계에서 제외한다

**구현**
- [x] `store/statementImport.ts` — 순수 함수만 (화면·파일시스템 의존 없음). 그래야 실제 파일로 바로 검증 가능
  - `StatementProfile` 구조 — 카드사별 열 이름 후보·취소 표시·합계행 판별. 프로필을 늘리면 다른 카드사도 지원
  - `parseDate` (점/하이픈/슬래시/8자리/엑셀 일련번호), `parseMoney`(부호 보존), `cardKeyOf`
  - `guessCategory` — 가맹점명 규칙 기반 (카카오모빌리티·택시 → 교통 등)
  - `buildCandidates` — 소유자 부여 + 중복 판정. **승인번호가 있으면 지문에 섞어 정확도를 높인다**
  - 수기 입력과 겹치는 건(날짜 ±1일 + 금액 동일)은 **표시만 하고 자동 병합하지 않는다**
- [x] `app/(tabs)/records/finance-import.tsx` — 파일 선택 → 카드별 사용자 지정 → 미리보기 → 확인
  - **웹에서는 드래그&드롭 지원** (데스크톱에서 파일 대화상자보다 빠르고, 자동 테스트도 가능해진다)
  - 건너뛰는 줄마다 이유 표시("취소된 거래라 넣지 않아요"), 중복 건은 "그래도 넣기"로 강제 추가 가능
- [x] 가계부 상단에 "카드 명세서에서 불러오기" 진입점

**의존성**
- `xlsx` — **npm 대신 SheetJS 공식 CDN의 0.20.3을 쓴다.** npm 레지스트리는 0.18.5에서 멈춰 있고
  프로토타입 오염(CVE-2023-30533)·ReDoS(CVE-2024-22363)가 남아 있다.
  `package.json`에 `"xlsx": "https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz"`
- `expo-document-picker` ~14.0.8
- ⚠️ 이 프로젝트에서 **`pnpm add`가 package.json을 갱신하지 않는 일이 있었다**(lock만 수정됨).
  의존성 추가 후 `package.json`에 실제로 들어갔는지 확인하고, 안 들어갔으면 직접 적고 `pnpm install`

**검증 (실제 신한카드 명세서)**
- Node에서 파서 단독 검증: 열 자동 매칭 **7/7 성공**, 카드 4장 감지, 취소 3건·합계행 1건 제외
- **가져올 금액 1,136,172원 = 엑셀 하단 합계와 정확히 일치**
- **멱등성**: 같은 파일 두 번째 → 0건 추가 / 9건 중복 처리
- 웹 UI 전 과정: 드롭 → 미리보기(9 추가 / 3 취소) → 카드 지정(380*→민준) → 가져오기 →
  가계부 9월 지출 894,100 → 1,213,872원, 구성원별 지수 719,282 / 민준 494,590으로 분리
- ⚠️ `파일 고르기` 버튼은 OS 파일 대화상자를 열어 자동 검증이 불가능하다. **드롭 경로로 검증했고 둘은 같은 `loadBuffer`를 쓴다**

### 2026-09-22 가져오기 2차 — 기억하는 가져오기 + 하드코딩 제거

**설정 저장소 — `store/financeSettings.ts`**
- 저장 위치: 웹은 `localStorage`, 네이티브는 아직 메모리. 접근이 막혀도 죽지 않게 try/catch로 감쌌다
- ⚠️ **`@react-native-async-storage/async-storage` 설치를 시도했으나 실패했다.** pnpm이 lock만 고치고
  실제로 받아오지 않는 현상이 반복돼 의존성을 뺐다. 어차피 이 값들은 **가족 공유 설정**이라
  Supabase를 붙일 때 DB로 옮기는 게 맞다 (기기별 저장은 임시)
- ⚠️ **`pnpm add`가 package.json을 갱신하지 않는 일이 잦다.** 게다가 **Metro 개발 서버가 떠 있으면
  node_modules 파일을 잡고 있어 설치가 완료되지 않는다**(`xlsx_tmp_*` 같은 임시 폴더가 남는 게 증거).
  의존성을 추가할 때는 **개발 서버를 먼저 끄고**, package.json에 직접 적은 뒤 `pnpm install`

**네 가지 모두 구현**
- [x] **카드 → 구성원 매핑 저장.** 한 번 지정하면 다음 가져오기 때 자동으로 붙는다
- [x] **열 직접 지정 + 프로필 저장.** 필수 열(거래일·가맹점명·금액)을 못 찾으면 열 맞추기 화면이
  자동으로 뜬다. 헤더 이름을 이어붙인 **지문(`headerSignature`)**으로 같은 모양의 파일을 알아본다
  - 저장하는 프로필은 함수를 담지 않는 `SavedProfile`(열 이름만) — 그대로 직렬화된다
  - `resolveColumns(headers, profile, override)` — 사용자 지정이 자동 매칭보다 우선
- [x] **가맹점 → 카테고리 교정 학습.** 미리보기에서 카테고리를 눌러 고치면 정규화된 가맹점명을
  키로 기억하고, 다음 가져오기 때 고정 규칙보다 먼저 적용된다
- [x] **할부 정책.** `이용구분`에서 개월 수를 읽고(`parseMonths`), 정책에 따라 처리한다
  - `full`: 결제한 달에 전액 (명세서 숫자와 같아진다) — 기본값
  - `split`: 매달 나눠서. **나누어떨어지지 않는 금액은 첫 달에 몰아** 합계가 원금과 정확히 맞는다.
    회차마다 지문이 달라야(`|1/3`) 중복 판정이 제대로 된다
  - ⚠️ **제공받은 명세서에 할부 건이 없어 실제 할부 행으로는 검증하지 못했다.** 실제 할부가 찍힌
    명세서가 생기면 `이용구분` 표기를 확인할 것

**하드코딩 제거**
- [x] 홈 "최근 기록" → `useRecentRecords(4)`. 카테고리 무관 최신순, 날짜는 `relativeDay`로 계산.
  카테고리별 아이콘·색·이동할 화면은 `CATEGORY_UI` 표로 관리
- [x] 기록 허브 개수 배지 → `useRecordCounts()`. 총 97개(가짜) → 58개(실제)
- [x] 기록 허브 라우팅을 절대경로 → 상대경로로 (CLAUDE.md 컨벤션에 맞춤)

**검증 (웹, 콘솔 에러 0건)**
- 홈 최근 기록: 육아일기·독서·영화·여행이 실제 최신순으로 표시
- 기록 허브: 총 58개, 가계부 17개(시드 2개월치와 일치)
- 카드 매핑: `380 → 민준` 지정 → localStorage 저장 확인 → **새로고침 후 다시 넣으니 LG U+ 건이 자동으로 민준**
- 카테고리 교정: `ANTHROPIC* CLAUDE SUB`를 교육 → 기타로 고침 → 저장 확인 →
  **새로고침 후 다시 넣으니 자동으로 기타**
- 열 맞추기 화면: 8개 항목 표시, 필수 3개에 `*` 표시

### 결제내역 자동 가져오기 — 나머지 단계 (미착수)
운영자가 "어디랑 연동시키면 자동으로 결제 내역을 긁어오는" 기능을 요청했다.
한국에서 계좌·카드 내역 조회는 **마이데이터(본인신용정보관리업) 허가** 또는 금융기관 제휴가
필요한 영역이라 개인 앱이 바로 붙일 수 없다. **요건은 매년 바뀌므로 착수 전 최신 기준을 확인할 것.**
현실성 순서로 정리하면:

| 단계 | 방법 | 현실성 | 비고 |
|---|---|---|---|
| 1 | 카드사/은행 명세서 **CSV·엑셀 가져오기** | ✅ 신한카드 완료 | 허가 불필요. 파일 선택 → 열 매핑 → 미리보기 → 일괄 등록 |
| 2 | **결제 알림 문자 파싱** | Android만 | iOS는 SMS 접근 불가. Android도 스토어 심사에서 SMS 권한이 까다로움 |
| 3 | **이메일 영수증 파싱** (Gmail API) | 가능하나 품질 편차 | 카드사 이메일 명세서를 받는 경우에 한함 |
| 4 | **오픈뱅킹 API** (금융결제원) | 사업자 등록 필요 | 개인 개발자는 어려움 |
| 5 | **마이데이터 사업자 연동** | 사실상 불가 | 금융위 허가·자본금 요건 |

> 1번(CSV 가져오기)이 비용 대비 효과가 압도적이다. 월 1회 카드사에서 명세서를 내려받아
> 넣으면 수기 입력 대부분이 사라진다. 이 기능을 먼저 만들고, 그래도 부족하면 2~3번을 검토한다.

**작성 폼 모달에 스크롤 적용 (전 화면)**
- [x] 작성 폼 모달 6개(육아일기·독서·가계부·영화·건강·타임캡슐)를 `<ScrollView style={{ maxHeight: 520~540 }}>`로 감쌌다. 여행·레시피·목표는 이미 그렇게 돼 있었고, 9개 전부 같은 방식으로 통일
  - **근거:** 375x667(아이폰 SE급) 화면에서 가계부 작성 폼 내용 높이가 703px로, 스크롤이 없으면 시트가 화면을 넘겨 아래쪽 입력칸과 저장 버튼에 닿을 수 없다. 폼이 짧은 화면(타임캡슐 458px 등)은 당장 필요하진 않지만 일관성과 향후 필드 추가를 위해 함께 적용
  - **작성 폼에 입력칸을 추가하면 375x667에서 저장 버튼까지 닿는지 확인할 것.** 데스크톱 창에서는 세로가 길어 드러나지 않는다

> ⚠️ **검증 함정 — 바텀시트가 화면 밖으로 밀려 보이는 현상.**
> 작업 중 "타임캡슐 저장 버튼이 화면 밖 64px 아래에 있다"고 판단해 커밋 메시지(52c023a)에
> 버그로 적었으나, **오진이었다.** 실제 원인은 미리보기 브라우저 창이 그리기를 멈추면
> `requestAnimationFrame`이 정지하고, React Native Web의 JS 기반 `Animated`가 시작값에
> 얼어붙는 것이다. 바텀시트는 `translateY: 500`에서 0으로 슬라이드해 올라오므로,
> 얼면 정확히 500px 아래에 멈춘 채로 측정된다.
> **판별법:** 시트 요소의 computed transform이 `matrix(1,0,0,1,0,500)`이면 애니메이션이 멈춘 것이고
> 앱 버그가 아니다. 스크린샷이 타임아웃되는 상황과 같이 나타난다. 창을 앞으로 가져오거나
> 페이지를 다시 열어 `matrix(1,0,0,1,0,0)`이 되는지 확인한 뒤 판단할 것.
- [x] 홈 화면 "최근 기록"을 `useRecentRecords()`에 연결
- [x] 기록 허브 카드의 개수 배지를 `useRecordCounts()`에 연결

**검증 메모**
- 웹에서 실제 입력·저장으로 확인 완료: 여행(속초·통영), 영화(어느 가족 — 평점 정렬까지), 레시피(할머니 장조림 — 재료 5줄·조리순서 3단계 배열 변환 확인)
- 브라우저 콘솔 에러는 **탭 단위로 누적**되므로, 파일 저장 중에 찍힌 과거 `TransformError`가 새로고침해도 계속 보인다. 진짜 에러인지 가르려면 **새 탭**에서 확인하거나 Metro 서버 로그를 볼 것

> 이 스프린트가 끝나면 기록은 앱을 켜 있는 동안 유지된다. 새로고침하면 사라지므로, 영속성은 다음 Supabase 스프린트에서 해결한다.

### 2026-09-23 Supabase 스프린트 — 1단계: 스키마 정비

**배경.** `supabase/migrations/`의 SQL이 앱 구조와 어긋나 있었다.
카테고리 9개 중 2개(육아·독서)만 테이블이 있었고, 앱이 쓰는 공용 기록 구조
(`data` JSON 봉투)와 가계부의 `ownerMember`/`source`/`importKey`가 빠져 있었다.
**아직 어느 DB에도 적용한 적이 없어** ALTER가 아니라 파일을 다시 썼다.

**🔴 기존 SQL에 있던 버그 — RLS 무한 재귀**
`family_members` 정책 안에서 `family_members`를 다시 조회하고 있었다.
그 조회에 또 같은 정책이 걸려 무한 재귀가 난다 (Supabase에서 흔한 함정):
```
ERROR: infinite recursion detected in policy for relation "family_members"
```
→ `my_family_ids()` **SECURITY DEFINER** 함수로 고리를 끊었다. 모든 정책이 이 함수를 쓴다.

**스키마 구조 — 카테고리별 테이블 대신 공용 `records` 한 장**
앱의 `FamilyRecord` 구조를 그대로 옮겼다. 이유:
1. 9개 카테고리는 데이터 모양이 전부 달라 공통 송장 + 자유 내용물이 맞다 (앱에서 이미 검증됨)
2. 테이블을 9개로 쪼개면 RLS 정책도 9벌이고, 공용 스토어를 카테고리별로 갈라야 해서 크게 다시 써야 한다
3. 카테고리를 늘릴 때 마이그레이션이 필요 없다
> 집계는 지금 앱이 화면에서 계산한다. 가족 한 집 규모면 충분하고, 느려지면 인덱스나 VIEW를 추가한다.

- [x] `00001_create_families.sql` — families / family_members
  - **RLS 재귀 수정** (`my_family_ids()`)
  - `UNIQUE(family_id, display_name)` 추가 — 기록의 `recordedBy`/`ownerMember`가 이름으로 사람을 가리키므로 가족 안에서 이름이 겹치면 안 된다
  - 정책을 `FOR ALL` 하나가 아니라 select/insert/update/delete로 나눠 권한을 정확히
- [x] `00002_create_records.sql` — **`records` 공용 테이블** + `calendar_events`
  - `records(id, family_id, category, title, created_by, recorded_by, data JSONB, import_key, created_at, updated_at)`
  - ⭐ **`UNIQUE(family_id, import_key) WHERE import_key IS NOT NULL`** — 가계부 중복 방지의 **마지막 방어선**.
    앱 화면에서도 거르지만, **세 사람이 같은 명세서를 동시에 넣는 경우는 화면 검사로 막을 수 없다.** DB가 막는다
  - 인덱스: 카테고리별 최신순 / 홈 최근기록 / 가계부 날짜(부분 인덱스)
  - `updated_at` 자동 갱신 트리거
  - 기존 `transactions` / `parenting_entries` / `reading_entries` 테이블은 `records`로 흡수돼 사라졌다
- [x] `00003_create_finance_settings.sql` — 가계부 설정을 **가족 공유**로
  - 지금 localStorage에 있는 카드매핑·열프로필·카테고리교정·할부정책·반복거래·예산을 가족당 한 줄로
  - ⚠️ 두 사람이 동시에 고치면 나중 것이 덮는다. 문제가 되면 `jsonb_set` 부분 갱신으로
- [x] `packages/core/src/types/database.ts` — 새 스키마에 맞춰 타입 재작성
  - `RecordRow<T>` / `FinanceSettingsRow` 추가, `Transaction`·`ParentingEntry`·`ReadingEntry` 제거
  - **DB는 snake_case, 앱은 camelCase.** 변환은 앞으로 `packages/core/src/supabase/records.ts` 한 곳에서만 할 것
- [x] `packages/core/src/utils/permissions.ts` — `Feature`를 실제 카테고리 9종에 맞춤,
  스키마에서 뺀 `Visibility` 의존 제거

**아직 넣지 않은 것 (의도적)**
- 역할 기반 RLS(아이에게 가계부·건강 숨기기) — 앱이 아직 역할을 쓰지 않고, 정책을 먼저 조이면 개발 중에 계속 막힌다. 인증을 붙인 뒤 별도 마이그레이션으로

### TODO — 다음 스프린트 (Supabase 연동)
- [ ] **운영자 작업:** Supabase 프로젝트 생성 → URL·**Publishable 키**를 `apps/mobile/.env`에 기입
      - Connect 창: `https://supabase.com/dashboard/project/_?showConnect=true` (`_`는 내 프로젝트로 자동 연결)
      - 키만: `https://supabase.com/dashboard/project/_/settings/api-keys`
      - Project URL은 언제나 `https://<ref>.supabase.co`. ref는 대시보드 주소창에 보인다
      - ⚠️ **`anon` 키(`eyJ...`)는 2026년 말 폐지 예정.** `sb_publishable_...`를 쓴다.
        `eyJ`로 시작하는 긴 JWT를 안내하는 글(과거 이 문서 포함)은 옛 방식이다
      - Secret 키(`sb_secret_...`)는 RLS를 우회하므로 앱에 절대 넣지 말 것
- [ ] 마이그레이션 3개 적용 (Supabase 대시보드 SQL Editor 또는 `supabase db push`)
- [x] **Supabase 연결 확인** — 프로젝트 생성·`.env` 기입·마이그레이션 적용 완료. 테이블 5개 응답 확인
- [x] `packages/core/src/supabase/records.ts` — DB ↔ 앱 변환 계층 (snake_case ↔ camelCase)
- [x] `packages/core/src/supabase/family.ts` — 가족 만들기 / 초대 코드 합류 / 조회
- [x] `00004_join_family_by_code.sql` — 초대 코드 합류용 DB 함수
      (아직 그 가족에 속하지 않아 RLS가 `families` 조회를 막는다. 닭과 달걀이라 SECURITY DEFINER 함수가 대신 한다)
- [x] `apps/mobile/store/session.ts` — 로그인한 사람 + 가족 + 구성원.
      `useMemberNames()` / `useCurrentUserName()`이 기존 `MEMBERS` / `CURRENT_USER` 상수를 대신한다

> 🔴 **TypeScript 함정 — `interface`는 supabase-js 타입에 못 쓴다.**
> DB 타입을 `export interface Family {...}`로 쓰면 supabase-js가 요구하는
> `Record<string, unknown>`에 대입되지 않는다. **interface에는 암묵적 인덱스 시그니처가 없기 때문**이다.
> 그러면 스키마 전체가 `never`로 떨어져 **insert/update가 통째로 막힌다**
> (`Argument of type '{...}' is not assignable to parameter of type 'never'`).
> → DB 행 타입은 반드시 **`export type X = {...}`**. 그리고 각 테이블에 `Relationships: []`도 필요하다.
> 증상이 헷갈리는 이유: 에러가 `client.ts`가 아니라 **호출부**에서 나고, 원인이 타입 선언 방식이라 보이지 않는다.
- [ ] 기록 스토어를 Supabase 연동으로 전환 (`store/records.ts` 안쪽만 교체)
- [ ] `store/seed.ts` + `_layout.tsx`의 `seedRecords()` 삭제
- [ ] 가계부 설정을 localStorage → DB로 이전
- [ ] 인증 가드 복원 (로그인 → 홈 플로우 실제 작동)
- [ ] 기록 CRUD 실제 데이터 저장/수정/삭제
- [ ] 온보딩 플로우 활성화 (가족 생성 / 초대 코드)

### TODO — 우선순위 보통
- [ ] 데이터 내보내기 실제 구현 (PDF/JSON/미디어)
- [ ] 이미지/미디어 업로드 구현
- [ ] Google Calendar 양방향 동기화

### TODO — 우선순위 낮음
- [ ] EAS Build 설정 (iOS/Android 스토어 배포)
- [ ] 푸시 알림 연결
- [ ] 연말 리포트 AI 생성
- [ ] 오늘의 질문 AI 생성 (크론)
